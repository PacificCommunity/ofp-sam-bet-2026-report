# Optional figure and table catalogues

The report does not generate or copy assets from another task. Current images
are committed in `Figures/`, current tables are committed in `tables/`, and the
ordered report content is controlled directly in `sections/Figures.qmd` and
`sections/Tables.qmd`.

The CSV catalogues in this directory are optional drafting aids. They are not
required to render the report.
